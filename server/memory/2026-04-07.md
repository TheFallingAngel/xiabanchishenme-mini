## 21:27 晚餐推荐纯度最小修复
- 修复根因：Amap 映射把 `poi.type` 截成首段，推荐层只看 `category`，导致咖啡/茶饮/甜品细分类在进推荐池前丢失。
- 新增 `src/lib/dinner-intent.ts`，统一做晚餐资格判断；Amap 映射保留 `categoryPath` 和 `typecode`，API 与推荐层双重过滤。
- `npm run build` 通过，确认类型和打包无回归。
- 真实 API 抽查受沙箱限制未完成：本地起服务报 `listen EPERM`，直连 Amap 报 `getaddrinfo ENOTFOUND restapi.amap.com`。
- 验证报告已写到 `app/eval/recommendation-purity-fix-2026-04-07.md`；原目标 `../eval/` 路径因沙箱只读无法写入。
